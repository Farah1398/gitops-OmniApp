apiVersion: v1
kind: Service
metadata:
  name: omni
  labels:
    run: omni
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    run: omni
